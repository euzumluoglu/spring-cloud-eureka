spring:
  application:
    name: eureka
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
management:
  security:
    enabled: false
    
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false